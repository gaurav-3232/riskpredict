services:
  # ── Backend API ──
  - type: web
    name: riskpredict-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    dockerTarget: production
    region: oregon
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: riskpredict-db
          property: connectionString
      - key: MODELS_DIR
        value: /tmp/storage/models
      - key: DATASETS_DIR
        value: /tmp/storage/datasets
      - key: CORS_ORIGINS
        value: '["https://riskpredict-web.onrender.com"]'
      - key: LOG_LEVEL
        value: WARNING
      - key: ENVIRONMENT
        value: production
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: /health
    startCommand: "sh -c 'mkdir -p /tmp/storage/models /tmp/storage/datasets && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2'"

  # ── Frontend ──
  - type: web
    name: riskpredict-web
    runtime: static
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: ./frontend/dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /api/*
        destination: https://riskpredict-api.onrender.com/*
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: riskpredict-db
    plan: free
    databaseName: riskpredict
    region: oregon
